{
  "name": "snaking",
  "version": "0.0.1",
  "private": true,
  "devDependencies": {
    "shadow-cljs": "2.15.5"
  },
  "scripts": {
    "dev": "npx shadow-cljs watch dev",
    "release": "rm -rf release && mkdir release && cp public/index.html release && cp -r public/resources release/resources && npx shadow-cljs release prod"
  },
  "dependencies": {
    "standardized-audio-context": "^25.3.6"
  }
}
